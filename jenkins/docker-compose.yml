version: "3.9"

services:
  jenkins:
    build: .
    container_name: jenkins
    user: root
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - ./jenkins_home:/var/jenkins_home   # 👈 ESTE ES EL CAMBIO
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always